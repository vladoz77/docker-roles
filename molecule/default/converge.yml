# molecule/default/converge.yml
- name: Converge
  hosts: all
  # become: true
  vars:
    docker_users:
      - molecule
    docker_daemon_options:
      log-opts:
        max-size: "10m"

  roles:
    - role: "docker-roles"